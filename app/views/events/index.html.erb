<p style="color: green"><%= notice %></p>

<h1>Events</h1>

<div id="events">
  <% @events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to "Show this event", event %>
      <%= button_to "Join Event", event_members_path(:event_id => event.id, :user_id => 1)%>
    </p>
  <% end %>
</div>

<%= link_to "New event", new_event_path %>
